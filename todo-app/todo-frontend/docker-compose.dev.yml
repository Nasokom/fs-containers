services: 
  app:
    image: todo-front-dev
    build: 
      context: .
      dockerfile: dev.Dockerfile

    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules


    #App accessible only via nginx reverse-proxy
    # ports:
    #    - 5173:5173

    container_name: todo-front-dev
  
volumes:
  node_modules:
